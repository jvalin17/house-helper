# Requirements: Job Agent v2 — Autonomous Job Application Agent

> Generated by /requirements on 2026-04-25
> Mode: standard
> Scope: major feature expansion of existing job agent
> User level: 3 (developer)
> Project state: existing codebase (223 backend tests, React frontend)
> Previous: requirements/job-agent.md (v1 — manual tool)

## Problem Statement

The current job agent is a manual tool — user pastes links, clicks generate, clicks apply. The real goal is an **autonomous job application agent** that auto-searches, auto-builds resumes, auto-applies (with confirmation), and auto-tracks. It must work in three LLM modes (no LLM, offline LLM, online LLM) and never exceed the user's token budget without permission.

## Core Requirement

**Auto-apply pipeline:** Search → Match → Generate tailored resume + cover letter → Preview → User confirms → Apply → Track. The agent does the work, the user reviews and approves.

## Boundaries

- **Excluded:**
  - No payment processing
  - No cloud storage — all data stays local
  - Never fabricate resume content — evidence-based only
  - Never exceed token budget without explicit user permission
- **Constraints:**
  - Existing stack: Python/FastAPI backend, React/TS frontend, SQLite, Tauri
  - All three LLM modes are first-class, not afterthoughts
  - Auto-apply must always end with user confirmation ("Thank you for applying")

## User Stories

- As a job seeker, I want the agent to find relevant jobs automatically so I don't spend hours searching job boards.
- As a job seeker, I want the agent to build a tailored resume for each job so every application highlights my best match.
- As a job seeker, I want to review the resume and cover letter before applying so I maintain control.
- As a job seeker, I want the agent to apply to up to 5 jobs at a time so I can batch my applications efficiently.
- As a job seeker, I want to track which approach leads to interviews so the agent gets smarter over time.
- As a job seeker, I want to control how many LLM tokens are used so I don't get surprise bills.
- As a job seeker, I want the app to work without any LLM so I can use it even without an API key.

## Functional Requirements

### 1. Auto Search

Search for jobs based on user skills + filters. Works in all 3 modes.

| Capability | No LLM | Offline LLM | Online LLM | Priority |
|-----------|--------|-------------|------------|----------|
| Search by skills from knowledge bank | Keyword matching against job board APIs | Semantic skill expansion ("Python" → also search "backend") | Same + LLM refines search terms | must |
| Search by user filters (title, location, remote, salary) | Direct API query params | Same | Same | must |
| LinkedIn API integration | API call with keyword filters | Same | Same | must |
| Indeed API integration | API call with keyword filters | Same | Same | must |
| Scheduled auto-search (user sets frequency) | Cron-style scheduler, runs when app is open | Same | Same | must |
| Search results deduplication | URL + title matching | Same + semantic dedup | Same | should |
| New job notifications | List new matches since last search | Same | Same | should |

### 2. Auto Resume Builder

Generate tailored resumes by intelligently selecting and rewriting content from the knowledge bank.
Evidence-based only — never fabricate. Format is fixed (user's original resume template).

#### 2a. Knowledge Bank Enrichment

The knowledge bank is the source of ALL resume content. Users enrich it with:
- Uploaded resume(s) — parsed into experiences, skills, education, projects
- Side projects — descriptions, tech stack, impact
- Work not on resume — achievements, metrics, responsibilities from any role
- Certifications, courses, volunteer work

All data is deduplicated. The richer the knowledge bank, the better the resume.

| Capability | Priority |
|-----------|----------|
| Import multiple resumes (dedup automatically) | must |
| Add side projects with descriptions and tech stack | must |
| Add achievements not on any resume (extra metrics, impacts) | must |
| Dedup: same company + overlapping dates = skip experience | must |
| Dedup: same institution = skip education | must |
| Dedup: same project name = skip project | must |
| Dedup: same skill name (case-insensitive) = skip | must |

#### 2b. Resume Generation Logic

Template assembly approach: Claude returns JSON decisions, our code assembles the final document.

**Step 1: Match knowledge bank vs job posting**
- Score ALL knowledge bank entries (experiences, projects, achievements) against the job
- Rank by relevance to the job's required skills and description

**Step 2: Select content for resume**
- Start with user's original resume format as the fixed template
- For each experience section: pick the TOP bullets by relevance
- If knowledge bank has MORE relevant bullets (from other work, side projects, unresumed achievements): SWAP less relevant bullets with more relevant ones
- Max bullets per role: 6 (configurable). Replace weakest with strongest from knowledge bank.
- If a side project matches better than a listed project: swap it in

**Step 3: Reword for emphasis**
- Reword selected bullets to emphasize skills matching the job
- Adjust summary for the target role
- Keep all facts true — only change phrasing, not content

**Step 4: Re-evaluate match**
- After generating: re-score the NEW resume against the job
- Show: "Match improved from 63% to 73% (+10%)"
- If no improvement possible: "Best achievable match: 65%. Gaps: [missing skills]"

**Step 5: Suggest improvements**
- "Adding Docker experience would increase match by ~8%"
- "Mentioning distributed systems in your Dematic role would help"
- "Consider adding a project using [missing skill]"

| Capability | No LLM | Offline LLM | Online LLM | Priority |
|-----------|--------|-------------|------------|----------|
| Select best bullets from knowledge bank | TF-IDF + skill overlap ranking | Sentence Transformers ranking | Claude ranks + rewords | must |
| Swap less relevant bullets with more relevant | Automated by match score | Same | Same + explains why | must |
| Reword bullets for job emphasis | Template only (no reword) | Local model rewords | Claude rewords | must |
| Re-evaluate match after generation | Algorithmic re-score | Same + semantic | Same + LLM re-score | must |
| Show match improvement | "Match: 73% (+10% from original)" | Same | Same | must |
| Suggest what to add to knowledge bank | Skill gap analysis | Same | LLM suggestions | should |
| Fixed template format | User's uploaded resume format — never changes | Same | Same | must |
| Export PDF/DOCX/TXT/MD | WeasyPrint, python-docx | Same | Same | must |
| Version history | Save each generated resume with job + preferences | Same | Same | should |

#### 2c. Resume Builder Decisions (from user)

| Decision | Choice | Date |
|----------|--------|------|
| Swap reasoning | Show WHY each swap was made ("Replaced bullet #5 with Docker project — +5% match") | 2026-04-28 |
| Page length | User chooses per generation: "Keep to 1 page" or "Allow 2 pages" | 2026-04-28 |
| Side projects placement | Separate "RELEVANT PROJECTS" section between experience and education when side projects match well | 2026-04-28 |

### 3. Auto Cover Letter Builder

Generate tailored cover letters. Same guardrails as resume.

| Capability | No LLM | Offline LLM | Online LLM | Priority |
|-----------|--------|-------------|------------|----------|
| Generate from knowledge bank + job | Template fill-in | Local model drafts | Cloud LLM drafts | must |
| In-UI editing | Editable textarea | Same | Same | must |
| Tone control | Template variants (formal/conversational) | Model parameter | Model parameter | should |
| Export all formats | Same as resume | Same | Same | must |

### 4. Auto Apply

Apply to jobs with user confirmation. Max 5 per batch.

| Capability | No LLM | Offline LLM | Online LLM | Priority |
|-----------|--------|-------------|------------|----------|
| Email-based apply | Compose email with resume + cover letter attached, user reviews and sends | Same + LLM personalizes email body | Same | must |
| Browser-based apply | Open job application page in browser, pre-fill where possible | Same | Same | must |
| Manual fallback | Show "apply at [URL]" with docs ready to download | Same | Same | must |
| Batch apply (up to 5) | Queue 5 jobs, generate docs for each, present for review | Same | Same | must |
| Always show match % | Display match score before apply | Same | Same | must |
| Confirmation message | "Thank you for applying to [Company]" after each successful apply | Same | Same | must |
| User must confirm each | No silent applications — user reviews and clicks confirm | Same | Same | must |

### 5. Auto Track

Track all applications with full audit trail.

| Capability | Input | Output | Priority |
|-----------|-------|--------|----------|
| Auto-track from apply flow | Confirmation from auto-apply | Application record with: job link, resume used, cover letter used, date, status, method (auto/manual) | must |
| Manual entry | User adds job link + status | Same record format | must |
| Track which features were used | Auto-search? Resume builder? Auto-apply? | Feature usage per application | must |
| Kanban board | All applications | Columns: Applied, Interview, Offer, Rejected | must |
| Status history timeline | Status changes | Timestamped log per application | must |
| Notes per application | User text input | Stored with application | should |
| Linked documents | Application record | Which resume + cover letter version was used | must |

### 6. Token Budget Manager

Control LLM usage across all features. Common pattern for all panini agents.

| Capability | Input | Output | Priority |
|-----------|-------|--------|----------|
| User sets token/cost limit | Settings: max tokens per day/session, or max $ | Budget enforced across all LLM calls | must |
| First-time setup prompt | App detects no budget set | Ask user: "How much would you like to spend on AI features?" with presets | must |
| Adaptive budget | Usage history | "Last week you used ~$0.50/day. Recommended budget: $1/day" | should |
| Priority queue | Multiple features requesting LLM | Resume builder gets priority, then search, then other tasks | must |
| Usage dashboard | Token consumption log | Show: tokens used today, remaining budget, breakdown by feature | must |
| Never exceed without permission | Budget reached | Pause LLM features, show: "Budget reached. Continue with free features or increase budget?" | must |
| Per-action cost estimate | Before LLM call | Show: "This will use ~X tokens (~$0.01). Proceed?" (configurable: always ask / ask over threshold / never ask) | should |

### 7. Knowledge Bank (updated from v1)

Existing — updated to support the auto pipeline.

| Capability | No LLM | Offline LLM | Online LLM | Priority |
|-----------|--------|-------------|------------|----------|
| Import resume (DOCX/PDF/TXT) | Drag-and-drop + file picker, regex parsing | Same + model improves extraction | Same | must |
| Free-text extraction | Regex skill patterns | Local model extracts structured data | Cloud LLM extracts | must |
| Manual CRUD | Forms for experiences, skills, education, projects | Same | Same | must |
| Skills grouped by category | Languages, frameworks, tools, soft skills | Same | Same | must |
| Education + projects display | Structured display | Same | Same | must |
| Evidence log | Track source of each knowledge entry (resume import, manual, user override) | Same | Same | must |

### 8. Three LLM Modes

Every feature must specify behavior in all three modes.

| Mode | What's Available | When to Use |
|------|-----------------|-------------|
| **No LLM** | Regex, TF-IDF, skill matching, template assembly, scraping | No API key, no internet, privacy-first |
| **Offline LLM** | Ollama/local models + all No LLM features | Has a computer with 8GB+ RAM, wants AI without cloud |
| **Online LLM** | Claude/OpenAI/HuggingFace + all offline features | Has API key, wants best quality |

| Capability | No LLM | Offline LLM | Online LLM |
|-----------|--------|-------------|------------|
| Resume generation quality | Functional — correct info, template layout | Good — tailored phrasing, contextual | Best — nuanced, ATS-optimized |
| Job search | Keyword only | Semantic keyword expansion | Smart query refinement |
| Job matching | Skill overlap + TF-IDF | + Sentence Transformers | + LLM deep analysis |
| Cover letter | Fill-in-the-blanks | Decent draft | Compelling, personalized |

## Non-Functional Requirements

| Category | Requirement | Target |
|----------|------------|--------|
| Latency | Resume generation (no LLM) | < 2 seconds |
| Latency | Resume generation (LLM) | 10-30 seconds acceptable |
| Latency | Job search | < 5 seconds per board |
| Latency | UI interactions | < 200ms |
| Storage | All data local | SQLite + filesystem |
| Security | API keys | OS keychain, never in DB |
| Security | Resume content | Evidence-based only, user additions logged separately |
| Privacy | User data | Never leaves device except LLM API calls + job board API calls |
| Privacy | Token usage | User always knows what's being sent to LLM |
| Budget | Token spending | Never exceed user's configured limit |
| Portability | Backup/restore | Copy ~/.panini directory |

## Data Requirements

| Data | Source | Stored? | Sensitive? | Notes |
|------|--------|---------|-----------|-------|
| Knowledge bank | Resume import + manual | Yes (SQLite) | Yes | Source-tracked (resume/manual/user-override) |
| Job postings | Job board APIs + manual paste | Yes (SQLite) | No | Parsed + raw |
| Search filters | User configuration | Yes (SQLite) | No | Title, location, remote, salary, keywords |
| Search schedule | User configuration | Yes (SQLite) | No | Frequency, last run, next run |
| Generated resumes | Template/LLM output | Yes (SQLite + filesystem) | Yes | Versioned, linked to job |
| Generated cover letters | Template/LLM output | Yes (SQLite + filesystem) | Yes | Versioned, editable |
| Applications | Auto-apply + manual | Yes (SQLite) | Low | Full audit trail |
| Token usage | Per-LLM-call tracking | Yes (SQLite) | No | Feature, tokens, cost, timestamp |
| Token budget | User settings | Yes (SQLite) | No | Daily/session limit |
| Evidence log | Knowledge bank operations | Yes (SQLite) | No | Source of each entry |
| LLM config | User settings | Yes (SQLite + keychain) | API keys are sensitive | Provider, model, budget |

## Assumptions

- LinkedIn and Indeed APIs may have access restrictions — build with plugin architecture so boards can be swapped
- Auto-apply via email requires user's email client or SMTP config — start with "compose in default mail app"
- Browser-based apply opens the URL — actual form-filling is post-MVP (requires browser automation)
- Scheduled search only runs when the desktop app is open (no background daemon for MVP)
- Token costs estimated based on current Claude/OpenAI pricing — will change over time

## Dependencies (new, in addition to v1)

- LinkedIn Jobs API or scraping library
- Indeed API (via RapidAPI or partner access)
- Email composition (mailto: links or SMTP)
- Scheduler (APScheduler or similar for Python)
- Token cost estimation (per-model pricing tables)

## Reusable from Current Codebase

| Module | Reusable? | Changes Needed |
|--------|-----------|----------------|
| `shared/db.py` | Yes | Add 4 new tables: search_filters, search_schedule, token_usage, evidence_log |
| `shared/algorithms/` | Yes | No changes |
| `shared/scraping/` | Yes | Add job board API clients |
| `shared/export/` | Yes | No changes |
| `shared/llm/` | Yes | Wrap with token budget tracking |
| `shared/calibration/` | Yes | No changes |
| `agents/job/repositories/` | Yes | Add new repos for new tables |
| `agents/job/services/` | Yes | Add auto_search, auto_apply, resume_guardrails services |
| `agents/job/prompts/` | Yes | No changes |
| `agents/job/routes.py` | Yes | Add new endpoints |
| `frontend/` | Needs redesign | Reorganize around auto-apply pipeline |

## Parking Lot

| Item | Category | Next Step |
|------|----------|-----------|
| Browser form-filling automation (Selenium/Playwright) | feature | Post-MVP — start with "open URL" |
| Background search daemon (runs when app closed) | feature | Post-MVP — needs system service |
| Multi-user support | feature | Not needed for personal tool |
| Job board plugins beyond LinkedIn/Indeed | feature | Architecture supports it, add as needed |
| Resume A/B testing (which version gets more interviews) | feature | Post-MVP learning loop enhancement |
| Interview prep agent | feature | Separate agent |

## Completeness

| Section | Status | Notes |
|---------|--------|-------|
| Problem Statement | :white_check_mark: | Clear — auto-apply is the core |
| Functional Requirements | :white_check_mark: | 8 feature groups with 3-mode specs |
| Non-Functional Requirements | :white_check_mark: | Budget + privacy + performance |
| Data Requirements | :white_check_mark: | 11 data entities |
| Scale Estimation | N/A | Personal desktop app |
| Infrastructure | N/A | Local only |
| Cost Estimate | N/A | Only LLM API costs (user-budgeted) |
